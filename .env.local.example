# Copy to .env.local for local dev (git-ignored). Without these two set,
# the Log page works exactly as before, just without cloud sync — nothing
# breaks. See supabase/schema.sql for the tables these point at.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
